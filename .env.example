# ------------------------------------------------------------------
# Veziit Environment Variables
#
# Create a .env file in the root directory and copy these variables
# into it, replacing the placeholder values with your actual keys.
# ------------------------------------------------------------------

# --- Firebase Client Configuration (for apps/web) ---
# These are safe to be publicly exposed on the client-side.
# Find these in your Firebase project settings.
VITE_FIREBASE_API_KEY=your_firebase_api_key
VITE_FIREBASE_AUTH_DOMAIN=your_firebase_auth_domain
VITE_FIREBASE_PROJECT_ID=your_firebase_project_id
VITE_FIREBASE_STORAGE_BUCKET=your_firebase_storage_bucket
VITE_FIREBASE_MESSAGING_SENDER_ID=your_firebase_messaging_sender_id
VITE_FIREBASE_APP_ID=your_firebase_app_id

# --- Firebase Cloud Messaging (FCM) ---
# This is the VAPID key for web push notifications.
# Find this in your Firebase project settings > Cloud Messaging > Web configuration.
VITE_FCM_VAPID_KEY=your_fcm_vapid_key

# --- Server-side Keys (DO NOT EXPOSE ON CLIENT) ---

# A secret key used for signing tokens or other server-side operations.
# Generate a strong, random string for this.
SERVER_MASTER_KEY=your_strong_secret_key

# --- Razorpay Payment Gateway ---
# Get these from your Razorpay dashboard (use sandbox keys for development).
# The Key ID is public, the Key Secret is private.
VITE_RAZORPAY_KEY_ID=your_razorpay_key_id
RAZORPAY_KEY_SECRET=your_razorpay_key_secret
